#function---Blokc of code that is designed
# to perform a specidifc task
#           built in cuntions
#           crea5e custom funtions
#           breas up our code into reusable parts
def say_hello(name):
    print("hello, " + name +"!" )

    say_hello('Adam')

    def add_numbers(x, y):
        return x + y
    def calculate_total(x, y, z):
        total = add_numbers (x,y)
        sum - add_numbers (x,y)
        return sum
    
    result = calculate_total(3,4,5)
    print(result)



    def say hello(name="world!")
    print()